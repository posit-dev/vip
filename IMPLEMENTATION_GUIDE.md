# Implementation Guide

This was an original planning document and has not been kept in sync with the
codebase. There is no equivalent page on the VIP website -- for current
architecture and setup guidance, see:

- **`AGENTS.md`** -- project overview, environment setup, test structure, the
  four-layer test architecture, and key source files.
- **`docs/development.md`** -- linting, type checking, the lockfile, and the
  release process.
