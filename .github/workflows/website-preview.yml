name: Website Preview

on:
  pull_request:
    types: [opened, reopened, synchronize, closed]

concurrency: website-preview-${{ github.ref }}

permissions:
  contents: write
  pull-requests: write

jobs:
  example-report:
    if: github.event.action != 'closed'
    uses: ./.github/workflows/example-report.yml
    secrets: inherit

  preview:
    if: github.event.action != 'closed'
    needs: example-report
    name: Build & deploy website preview
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Sync Python dependencies
        run: uv sync --frozen

      - name: Generate test catalog
        run: |
          if [ -f scripts/generate-test-catalog.py ]; then
            uv run python scripts/generate-test-catalog.py
          fi

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: website/package-lock.json

      - name: Build website
        run: |
          cd website
          npm ci
          npm run build -- --base /vip/pr-preview-site/pr-${{ github.event.pull_request.number }}/
          touch dist/.nojekyll

      - name: Download example report
        uses: actions/download-artifact@v4
        with:
          name: example-report
          path: website/dist/example-report/

      - uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: website/dist/
          preview-branch: gh-pages
          umbrella-dir: pr-preview-site
          comment: false

      - uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: preview-links
          message: |
            **Preview Links**
            - [Website](https://posit-dev.github.io/vip/pr-preview-site/pr-${{ github.event.pull_request.number }}/)
            - [Validation Report](https://posit-dev.github.io/vip/pr-preview/pr-${{ github.event.pull_request.number }}/)

  cleanup:
    if: github.event.action == 'closed'
    name: Clean up website preview
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: website/dist/
          preview-branch: gh-pages
          umbrella-dir: pr-preview-site
          comment: false
          action: remove

      - uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: preview-links
          delete: true
